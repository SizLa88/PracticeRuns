[pytest]
# Automatically applies log output parameters directly to your execution scripts
addopts = -s -v --tb=short

# Registers execution order markers to prevent console runtime warning logs
markers =
    order: specify execution order for tests
